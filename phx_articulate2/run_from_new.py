import subprocess

# Path to the script you want to run
script_path = "/home/scalepi/phx_articulate2/Pick_coord_from_crop_txt2.py"

# Run the script using subprocess
subprocess.run(["python", script_path])
